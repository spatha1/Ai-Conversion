# Data Conversion Studio — Python API dependencies
# Install: pip install -r api/requirements.txt

# ── Web framework ────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.1

# ── Settings / config ────────────────────────────────────────
pydantic-settings==2.6.1

# ── Database (project DB — SQL Server) ──────────────────────
sqlalchemy==2.0.36
pyodbc==5.2.0

# ── Encryption ───────────────────────────────────────────────
cryptography==43.0.3

# ── XML parsing (used by bootstrap_target_formulas.py) ───────
lxml>=5.0.0

# ── SQL source connectors (install what you need) ────────────
# SQL Server (already covered by pyodbc above)

# PostgreSQL:
# psycopg2-binary==2.9.9

# MySQL:
# pymysql==1.1.1

# ── Snowflake ────────────────────────────────────────────────
snowflake-connector-python==3.12.3

# ── AI Chat (OpenAI proxy) ───────────────────────────────────
openai>=1.0.0

# ── SQL AST parser (safe SQL validation) ─────────────────────
sqlglot>=23.0

# ── Dispatch — SFTP ──────────────────────────────────────────
paramiko>=3.4.0
pynacl==1.5.0          # pinned — 1.6.x requires cffi>=2.0 which conflicts with snowflake

# ── Dispatch — Azure Blob Storage ───────────────────────────
azure-storage-blob>=12.19.0

# ── Auth ─────────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# ── Document / URL parsing (Knowledge Base) ──────────────────
pypdf>=6.0.0
python-docx>=1.2.0
beautifulsoup4>=4.12.0

# ── UI Validation (Playwright — headless browser automation) ─
# After installing, run once: playwright install chromium
playwright>=1.44.0
